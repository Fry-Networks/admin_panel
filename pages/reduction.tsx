import { Title } from "@tremor/react";

export default function ReductionPage (reduction = []) {
    return (
        <main className="p-4 md:p-10 mx-auto max-w-8xl">
            <Title>Reduction</Title>
            
        </main>
    )
}